For all plans listed here, you will create sub agents for each task to conserve context. You are acting as a manager, making sure to move each agent along, and make sure that the tasks get completed fully, and to a production grade standard. Each task will be completed in four phases:

1. Planning phase: This agent should plan out in more detail what needs to happen. It should plan out what key components need to be built, how the files should be structured, what functionality exists and can be extended, and what needs to be created. It should make sure that it has a full understanding of the feature and what this feature may interact with, so that it can plan around those interactions. Essentially, it should create a detailed plan for the agents that will follow so that they can stick to a plan, and they can only be concerned with their primary task. It should not get too far into the technicalities of the plan (unless necessary), as each agent that follows will be specialized for a task, so it just has to create the outline, and the following agents will take care of the granular implementation details.
2. Design and UI phase: In the design phase, you will create a sub agent tasked with creating the page specified. It should use the vercel react best practices skill, and the frontend-design skill so that it is best equipped for the task. It should follow existing design standards and language. It should create a beautiful layout, while following any instructions given. It should leave all functionality that is not trivial to the agent in the next phase and marked with TODO comments. It should also leave comments for things that the functionality phase agent may want to know about. It should only do this when the point of some UI or function may be unclear.
3. Functionality phase: In the functionality phase, another agent will take over from what was done in the design phase, here the agent will create and connect any advanced functionality that is needed. This agent will be creating new backend routes, adding loading states, and making sure all of the UI that was built is fully functional. It should use the vercel react best practices skill. This agent should remove any comments left by the Design and UI agent. It should leave comments when code is unclear and needs a comment to explain it.
4. Review phase: This agent will review the work of both agents that came before it, it will look for things that should be optimized, potential bugs and edge cases, and just overall bad code design. It should use the vercel react best practices skill. Make sure that this agent does not overoptimize or prematurely optimize, it is simply looking for large gains that can be made easily. The code should be readable, not too verbose, not have any huge performance oversights, or bugs. But again, it is critical that this agent does not over optimize, it is simply here as a safety net to catch errors that the first two agents may have missed. If any comments were left it should remove them, unless the comments are making code that is truly unclear more clear. 

All four of the agents should follow existing standards in the codebase. 

We always use trpc for our backend routes, they are defined as:
packages\api\src\root.ts - root router
packages\api\src\routers\user-router.ts - router that has functions
packages\api\src\user\getUserInfo.ts - function for said router

New router should be created when we are building a new conceptual part of the app, so if we were adding a new admin part, we would create an admin router, and then put its functions in the "admin" folder.

Our database is defined by drizzle-orm in packages/db, when creating new files, follow the naming convention "concept-db.ts", like "users-db.ts", then, make sure to add it to the packages\db\src\schema.ts file. This can be imported from '@guitar-app/db'.

All zod schemas should be defined in packages\schemas\src\(concept)-zod.ts, and exported through packages\schemas\src\index.ts, the zod library itself should also be imported from "@guitar-app/schemas"

When each agent is done, make sure that it runs the `pnpm format` command in the root of the project, it does not need to keep the output of this command. 

We use supabase for our authentication, storage, and database provider. You can read the user object on the client from the useAuth hook, and from the trpc getUserInfo route on the server.

Make sure each task is marked as completed inside of dashboard-plans-progress.txt. The file should be updated by each agent in the format:

{task name 1} - {In progress/completed}
{last completed phase (1/2/3/4)}
{Last update timestamp}

{task name 2} - {In progress/completed}
{last completed phase (1/2/3/4)}
{Last update timestamp}

If there is anything that needs to be done outside of the codebase, like some external service settings, or api keys, write a note of it in @user-notes.txt

...

Tasks to complete:

1. We just updated our practice client in @frontend/components/learn/caged-practice-client.tsx to follow a new pattern. These are the tasks we previously completed: 

a. We have a practice client that teaches our users the caged system on the guitar in @frontend/components/learn/caged-practice-client.tsx that is used in @frontend\components\learn\learn-module-client.tsx . We are going to refactor the caged practice client and improve how it works to help users learn more effectively. First, we will start with the roots. Right now, the users read the article, then, they can start the practice, where it takes them through 2 rounds of playing the caged roots, and then they do the same thing in 3 keys. We are going to change this. First, we will take them through 3 rounds of playing the caged roots for in the key of D major. After they finish the first 3 rounds, we will show them a shorter article, this will be the same idea as the first article they see, where it is a react component representing an article, it will use visuals to illustrate the ideas. This article will basically just emphasize again the roots and why they are important, and how they go in order, and they each represent one of the letters of CAGED. At the end of that article, there will be a button to now try it out in a new key. Pressing this button will take them to the practice session again, but it will make them play 1 round in C major, one round in D# major, and then one round in F major. After that it will show them another short article about how we just played the same shapes in the same order, but moved them around, but how the order stayed the same. Finally, it will be the test mode, where they have to play in D Major, but they can't see the notes, they have to play them correctly for the notes to be revealed. In each short article, make sure there is a button to retry the practice session they did. Finally, we have a quiz at the end, keep the quiz as it is. Once they have finished the quiz, show them one more short article about how next up is chord tones. We will use this idea for other practice modes (short practice, then short article, then short practice ... ), not just the caged roots, so make sure that it is extensible. 

b. We just refactored our caged roots to use a new structure, the details are above. We are now going to transfer that to our chord tones. We added new steps to our practice where we would have the user play some of the caged roots in a key, then show an article, then we would have them practice more. We are going to do that now, when the user presses start the first time in our caged chord tones practice, they will only practice the chord tones for the C and A shapes in the key of D. They will do this twice, so they will practice C, then A, then C, then A. Then a short article will show, explaining why the chord tones are where they are, and just an overview. Then, it will show the shapes for G, E, and D, then the user will practice those twice, so they will do the chord tones for G, E, D, then again, G, E, and D. Finally, one more short article will show that is basically just another review, then they will practice every shapes chord tones once. Finally, it will start its testing mode, here, they cannot see the chord tones for the shape, and must play them to reveal them. It will however show the roots, and it will say the shape being played. Once they have completed that once for each shape, it will show them the quiz. Once they complete the quiz, it will give a recap, explain that for this, practice is very important, and then explain how the minor pentatonic is the next step.

We are now going to add the final piece to this, we are going to apply this to our minor pentatonic module. In this, we will use the same pattern of practice, then short article, and then back to practice, but with each pentatonic shape. We will start with our existing article of the minor pentatonic, then we will start with just the C shape. They will practice this shape 3 times. First, in the key of D, then E, and then G. Then, a short article reviewing that shape, and then showing the A shape, then they will practice that shape 3 times in the same keys. Then they will see a short article reviewing the A shape, then showing the G shape. This will repeat until they have done all 5 shapes. Then, they will be tested in the key of D. The practice board will hide all notes except the root they are currently playing. Then, it will start with the C shape, once they have played that, then A, and so on until they finish. Finally, it will show the quiz, and after that, one more short article reviewing this and then talking about how next up is the major scale. 

2. With what we just added, we are now going to track the users progress through these modules. We are focusing only on caged right now, but we will use this setup for all of our lessons in the future. We added a new table to our database defined by drizzle orm in @packages\db\src\schema\learning-db.ts . This references the users table in @packages\db\src\schema\users-db.ts . You will need to build out this table so that our database is able to track the information we need to. Make sure to use drizzle relations when you can. Do not worry about the SQL migrations, I will handle those, so that the types update, you can run pnpm build:packages in the project root to rebuild packages. Our lesson structure is as follows. There are paths, right now we only have one "caged", then within paths, there are lesson, in caged, we have three right now, roots, chord tones, and minor pentatonic. Within each lesson, we have modules, the modules are what we just built out, so for the caged roots, the first module is the roots in D major, then the article is the next module, then the roots in a different key is the next module. The test mode is another module, and the quiz at the end is another module, so is each article. Basically, every stage of the practice client for a topic is its own module. We will be tracking the users completion of these. For each module, we track if it was completed or not, as well as when it was completed. If all modules in a lesson have been completed, then we mark the lesson as complete. Since we are now tracking this, we also need to add some more navigation to our learning client, it should be a sidebar on the left that shows all of the modules within the lesson, the user can click to skip to a specific module, and each item shows the completion state. The one after the most recently completed module should also have some kind of visual indicator that it is the current module. For article modules, we will mark them as completed if the user presses the next button at the bottom of the article, for practice modules, we show it as completed whenever they finish the current practice requirements, for quizzes it is when they answer all questions correctly. 